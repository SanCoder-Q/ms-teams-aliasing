{
  "version": "0.0.1",
  "name": "ms-teams-aliasing",
  "author": "jmqu",
  "email": "sancoder.q@gmail.com",
  "private": true,
  "dependencies": {
    "archiver": "^7.0.1",
    "chrome-webstore-manager": "^0.5.0"
  },
  "scripts": {
    "build": "node ./ci/build.js",
    "update": "chrome-webstore-manager update -t ${TOKEN} ${EXTENSION_ID} ./dist/build/ms -teams-aliasing.zip",
    "publish": "chrome-webstore-manager publish -t ${TOKEN} ${EXTENSION_ID}"
  }
}
